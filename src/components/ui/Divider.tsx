export const Divider = () => {
  return (
    <div
      style={{
        width: "100%",
        marginBlock: "1em",
        borderTop: "1px solid var(--border-color)",
        opacity: 0.6
      }}
    />
  );
};
